<% include ../partials/header %>


    <div class="container">
        <div class="col-lg-12">
            <div class="content">
                <div class="col-lg-4">
                    <% friendships.forEach(function(friendships){ %>
                        <% if (friendships.status === "pending" ){%>
                            <div class="thumbnail">
                                <h4> <%=friendships.friend.username %></h4>
                            </div>
                        <% } %>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

<% include ../partials/footer %>